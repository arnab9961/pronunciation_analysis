[build-system]
requires = ["setuptools>=45", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "antonio"
version = "0.1.0"
description = "A Python project with upscale services"
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    # Add your dependencies here
]

